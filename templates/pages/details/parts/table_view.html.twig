<table id="table" class="table-nonfluid table table-hover table-sm table-bordered">
    <thead class="thead-dark">
    <tr>
        {% for header in table.headers %}
            <th>{{ header }}</th>
        {% endfor %}
        {% if is_granted('ROLE_ADMIN') or (app.user is not null and admin == app.user.email) %}
        <th>Úpravy</th>
        {% endif %}
    </tr>
    </thead>
    <tbody>
    {% for row in table.rows %}
        <tr>
            {% for data in row.data %}
                <td>{{ data }}</td>
            {% endfor %}
            {% if is_granted('ROLE_ADMIN') or (app.user is not null and admin == app.user.email) %}
            <td>
                <a href="#" class="btn btn-danger btn-sm remove-item" data-id="{{ id }}" data-row-id="{{ row.id }}" data-table="{{ table.name }}">Odebrat</a>
            </td>
            {% endif %}
        </tr>
    {% endfor %}
    </tbody>
</table>